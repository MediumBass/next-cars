import React from 'react';

const Loading: React.FC = () => (
  <div className="flex items-center justify-center h-full">
    <p className="text-lg  text-[3vw]">Loading...</p>
  </div>
);

export default Loading;
